/* global hasher */
sap.ui.define(['sap/fe/core/AppComponent'], function(AppComponent) {
    'use strict';

    return AppComponent.extend('cap.covid.ui.recovered.Component', {
        metadata: {
            manifest: 'json'
        }
    });
});
